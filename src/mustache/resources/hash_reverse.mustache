package systems.comodal.hash.gen;

import Hash;
import systems.comodal.hash.{{hash}};
import HashFactory;
import ReverseHash;

public final class Reverse{{hash}} extends ReverseHash implements {{hash}} {

  public Reverse{{hash}}(final byte[] data, final int offset) {
    super(data, offset);
  }

  @Override
  public HashFactory<{{hash}}> getHashFactory() {
    return FACTORY;
  }

  @Override
  public boolean equals(final Object other) {
    return this == other || other != null && other instanceof {{hash}}
        && ((Hash) other).digestEqualsReverse(data, offset);
  }
}