package systems.comodal.hash.base;

import systems.comodal.hash.Hash;
import systems.comodal.hash.{{hash}};

public final class LittleEndianOffset{{hash}} extends LittleEndianOffsetHash implements {{hash}} {

  public LittleEndianOffset{{hash}}(final byte[] data, final int offset) {
    super(data, offset);
  }

  @Override
  public boolean equals(final Object other) {
    return this == other || other != null
        && other instanceof {{hash}}
        && ((Hash) other).equalsReverse(data, offset);
  }
}
