package systems.comodal.hash.gen;

import systems.comodal.hash.api.Hash;
import systems.comodal.hash.{{hash}};
import systems.comodal.hash.api.HashFactory;
import systems.comodal.hash.base.DiscreteHash;

public final class {{hash}}Value extends DiscreteHash implements {{hash}} {

  public {{hash}}Value(final byte[] data) {
    super(data);
  }

  @Override
  public HashFactory<{{hash}}> getFactory() {
    return FACTORY;
  }

  @Override
  public boolean equals(final Object other) {
    return this == other || other != null && other instanceof {{hash}}
        && ((Hash) other).digestEquals(data);
  }
}